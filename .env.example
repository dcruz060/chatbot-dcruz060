# Configurar en Vercel → Settings → Environment Variables (Production + Preview)
# Al guardar como "Sensitive", Vercel oculta el valor; al editar veras el campo vacio (es normal).

GENAI_URL=https://genai-sharedservice-americas.pwcinternal.com/chat/completions
GENAI_API_KEY=
GENAI_MODEL=vertex_ai.gemini-2.5-flash

# Desarrollo local del frontend (opcional)
# VITE_API_URL=/api/chat
