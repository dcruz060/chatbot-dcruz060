# Variables para la Serverless Function /api/chat (configurar en Vercel → Settings → Environment Variables)
GENAI_URL=
GENAI_API_KEY=
GENAI_MODEL=

# Desarrollo local del frontend (opcional; por defecto usa /api/chat con proxy de Vite)
# VITE_API_URL=/api/chat
